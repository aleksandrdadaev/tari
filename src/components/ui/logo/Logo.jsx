import styles from './logo.module.scss'

const Logo = () => {
	return (
		<div className={styles.logo_wrapper}>
			<img src='' alt='' className={styles.logo_image} />
		</div>
	)
}

export default Logo
